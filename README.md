# 🚀 FastAPI Signup API

A simple FastAPI project that provides a user signup endpoint. This API accepts user details via a `POST` request and returns a success message upon successful registration.

## 📌 Features
- ✅ FastAPI-based signup endpoint
- ✅ Accepts JSON requests with `name`, `username`, and `password`
- ✅ Returns a success message with the registered username
- ✅ Lightweight and easy to deploy

